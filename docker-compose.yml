services:
  vgmss:
    build: .
    image: vgmss:latest
    container_name: vgmss
    restart: unless-stopped
    ports:
      - "127.0.0.1:${VGMSS_PORT:-8220}:8080"
    volumes:
      - ./data:/data
